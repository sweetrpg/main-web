---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
    name: main-web-auth

spec:
    refreshInterval: "1h"
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: web-auth
        creationPolicy: Owner
    dataFrom:
        - extract:
              key: Auth0

---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
    name: main-web-misc

spec:
    refreshInterval: 1h
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: web-misc
        creationPolicy: Owner
    dataFrom:
        - extract:
              key: /sweetrpg/main/web/misc

---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
    name: main-web-cache

spec:
    refreshInterval: 1h
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: web-cache
        creationPolicy: Owner
    dataFrom:
        - extract:
              key: /sweetrpg/support/cache/client

---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
    name: main-web-files

spec:
    refreshInterval: 1h
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: web-files
        creationPolicy: Owner
        template:
            type: Opaque
            data:
                newrelic.ini: "{{ .contents | toString }}"
    data:
        - secretKey: contents
          remoteRef:
              key: NewRelic

---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
    name: main-template-credentials

spec:
    refreshInterval: 1h
    secretStoreRef:
        name: akeyless
        kind: ClusterSecretStore
    target:
        name: template-credentials
        creationPolicy: Owner
    dataFrom:
        - extract:
              key: TemplateRepo
