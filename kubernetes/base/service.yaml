---
kind: Service
apiVersion: v1

metadata:
    name: main-web-v1

spec:
    selector:
        app: main-web
        platform: sweetrpg
        package: main
        component: web
        version: v1
    type: ClusterIP
    ports:
        - name: http
          port: 8080
          targetPort: http
