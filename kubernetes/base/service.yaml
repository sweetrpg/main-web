---
kind: Service
apiVersion: v1

metadata:
    name: web-v1

spec:
    selector:
        app: web
        platform: sweetrpg
        package: main
        component: web
        version: v1
    type: ClusterIP
    ports:
        - name: http
          port: 8080
          targetPort: http
